# Cloud (Managed Services) Environment Configuration Example
# Copy to .env.cloud (or environment variables in your cloud platform) and adjust.
# Do NOT commit real secrets.

APP_NAME=enhanced-ai-agent-os
APP_ENV=production
PORT=5678
LOG_LEVEL=info

# Public base URLs (if applicable)
API_BASE_URL=https://api.your-domain.example
N8N_BASE_URL=https://n8n.your-domain.example

# Primary app database (prefer single URL)
DATABASE_URL=postgresql+psycopg2://USER:PASS@HOST:5432/DBNAME
# Optional component-style config (used if DATABASE_URL is unset)
DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=
DATABASE_SSL_MODE=require
DATABASE_SSL_ROOT_CERT=

# Neo4j (managed e.g., Aura) — use bolt+s for TLS
NEO4J_URI=bolt+s://YOUR_AURA_INSTANCE.databases.neo4j.io:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=

# RabbitMQ (optional, if using a managed MQ)
RABBITMQ_URL=amqps://USER:PASS@HOST:5671/VHOST

# Redis (optional)
REDIS_URL=rediss://HOST:6380

# Security & CORS
JWT_SECRET=
JWT_SECRET_KEY=${JWT_SECRET}
ENCRYPTION_KEY=base64:
HMAC_SECRET=
WEBHOOK_SECRET=
CORS_ORIGINS=https://your-frontend.example
ALLOW_ORIGINS=https://your-frontend.example
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# AI Providers (cloud keys)
AI_DEFAULT_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20240620
GOOGLE_API_KEY=
GOOGLE_MODEL=gemini-1.5-pro

# Observability (use managed stacks or SaaS)
ENABLE_PROMETHEUS=true
PROMETHEUS_PORT=9464
ENABLE_TRACING=true
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=enhanced-ai-agent-os
SENTRY_DSN=

# Notifications (optional)
PAGERDUTY_INTEGRATION_KEY=
SLACK_WEBHOOK_URL=

# Email (SaaS recommended)
EMAIL_PROVIDER=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=ai@your-domain.example

# Storage (cloud bucket for artifacts/backups)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=ai-agent-artifacts-prod

# Backup & DR
BACKUP_ENABLED=true
BACKUP_S3_BUCKET=ai-agent-backups-prod
BACKUP_SCHEDULE_CRON="0 2 * * *"
BACKUP_RETENTION_DAYS=30
DR_STRATEGY=multi-region-active-passive

# n8n basics (when running as a separate service)
N8N_API_KEY=
N8N_WEBHOOK_SECRET=

# Notes:
# - On platforms like Render/Heroku, prefer setting these as service env vars.
# - Our cloud migration scripts read DATABASE_URL and NEO4J_*.
